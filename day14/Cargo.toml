[package]
edition = "2021"
name = "day14"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.51"
itertools = "0.10.1"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
